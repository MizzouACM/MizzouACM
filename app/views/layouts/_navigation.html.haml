= link_to "Mizzouacm", root_path, :class => 'brand'
%ul.nav
  - if user_signed_in?

    %li
      = link_to 'Logout', signout_path
    %li.pull-right
      = link_to "Logged in as #{current_user.name}", edit_user_path(current_user)
  - else
    %li
      = link_to 'Login', signin_path
