.transparent-region
  - if user_signed_in?
    =link_to new_project_path, :class => "pull-right" do
      =icon_btn "Submit a Project", "plus"
  %h1 Member Projects
  #projects
    - @projects.each do |project|
      .project
        = link_to project do
          = image_tag project.photo.url(:thumb)
          .textFrame
            = project.title + ' by ' + project.user.name
  %br