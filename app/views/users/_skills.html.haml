
%h4 Skills

%ul
  -@user.skills.reject(&:new_record?).each do |skill|
    %li
      =Skill.getIcon(skill.name)
      =skill.name

- if user_is_current_user?
  %h5 Add a new skill
  = simple_form_for([@user, @new_skill]) do |f|
    = f.error_notification

    .form-inputs
      = f.input :name, collection: Skill.valid_skills_dropdown, default: 1, label: false

    .form-actions
      = f.button :submit, "Add Skill"
